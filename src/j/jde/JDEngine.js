/**
 * JavaScript Database Engine
 * @pakcage jdbe
 */
'package jdbe'.j(function () {
    /**
     * @class JDEngine
     */
    'class JDEngine'.j(function () {

        jprotected({

        });

        jpublic({
            constructor: function (jsdb) {
                this.jsdb = jsdb;
            }
        });

    });
});